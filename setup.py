from setuptools import setup, find_packages

setup(
    name='zeroheliumkit',
    version='0.4.2',
    packages=find_packages(include=['zeroheliumkit', 'src', 'helpers', 'fem']), 
    install_requires=[]
)
